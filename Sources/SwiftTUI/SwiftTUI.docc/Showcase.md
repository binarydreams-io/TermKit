# Showcase

Explore SwiftTUI components in a terminal application.

## Run The Showcase

Run the package executable:

```bash
swift run SwiftTUIShowcase
```

The showcase includes prompts, command palettes, messages, reasoning, tool output, diffs, permissions, questions, toasts, and sidebars. Press `q` or `Esc` to exit.
